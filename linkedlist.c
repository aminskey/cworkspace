
#include <stdio.h>
#include <stdlib.h>

#include "libs/node.h"

int main(void)
{
        

  struct Node *Head=initHeadNode(0);
  struct Node *_sec=addNode(Head);

  //  printLinkedList(Head);

  //freeNode(Head);
  //freeNode(_sec);

  return 0;

}
